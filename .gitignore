# Local tool/session state — not part of the app, shouldn't be public.
.claude/
.superpowers/
node_modules/

# Internal docs: session plans/specs and business/feasibility material are
# confidential. Only the app itself (code, assets, tests) belongs on GitHub.
docs/
